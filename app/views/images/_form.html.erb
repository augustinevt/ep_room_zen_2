<%= form_for [object, Image.new], :html => { :multipart => true }, remote: true do |f| %>
  <%= f.label :body %>
  <%= f.file_field :body %>

  <%= f.label :caption %>
  <%= f.text_field :caption %>

  <%= f.submit %>
<%end%>
