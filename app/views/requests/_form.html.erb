<div id="form-inner">
  <%= form_for [house, house.requests.new], authenticity_token: true, remote: true do |f| %>
    <%= f.label :message %>
    <br>
    <%= f.text_area :message %>
    <br>
    <%= f.submit 'Submit' %>
  <%end%>
</div>
